in adder we used workspace to manage different pakages there is only on target file that also in main file and we
cargo run -p adder suppose here -p is used to build any particular package
-------------------------Publishing Crate-----------------------------

you can only publish one package at a time 


-------------------------Installing Crate-----------------------------
cargo install is used to install the crate 
